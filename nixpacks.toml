# Configuración mínima para PHP
[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli"]

# No instalar dependencias - proyecto PHP puro
[phases.install]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT"

