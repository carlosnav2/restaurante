# Configuración para Nixpacks (usado por Railway)
[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php -S 0.0.0.0:$PORT"

